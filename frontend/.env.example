# Keep empty to route browser requests through Next.js at the same origin.
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_STUDENT_ID=development-student

# Server-only upstream used by next.config.mjs.
BACKEND_URL=http://127.0.0.1:8000
