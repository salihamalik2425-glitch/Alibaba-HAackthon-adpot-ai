"use client";

import { FormEvent, useState } from "react";
import { ArrowUp, Bot, LoaderCircle, Sparkles, UserRound } from "lucide-react";
import { askAdaptIQ } from "@/lib/api";

type Message = { role: "assistant" | "user"; content: string };
const starters = ["Explain Newton's Second Law", "Help me understand photosynthesis", "Quiz me on algebra", "Explain this step by step"];

export function AskChat() {
  const [question, setQuestion] = useState("");
  const [messages, setMessages] = useState<Message[]>([]);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState("");
  async function submitQuestion(event?: FormEvent) {
    event?.preventDefault(); const trimmedQuestion = question.trim(); if (!trimmedQuestion || isLoading) return;
    setMessages((current) => [...current, { role: "user", content: trimmedQuestion }]); setQuestion(""); setError(""); setIsLoading(true);
    try { const response = await askAdaptIQ(trimmedQuestion); setMessages((current) => [...current, { role: "assistant", content: response.answer }]); }
    catch (requestError) { setError(requestError instanceof Error ? requestError.message : "Something went wrong. Please try again."); }
    finally { setIsLoading(false); }
  }
  return <div className="grid gap-5 xl:grid-cols-[minmax(0,1.5fr)_350px]"><section className="flex min-h-[650px] flex-col rounded-3xl border border-slate-200 bg-white p-5 shadow-soft sm:p-7"><div className="flex items-start justify-between gap-5 border-b border-slate-100 pb-5"><div><div className="mb-2 flex items-center gap-2 text-xs font-bold uppercase tracking-[0.16em] text-primary"><span className="h-1.5 w-1.5 rounded-full bg-primary" /> Personal tutor</div><h2 className="font-display text-2xl font-bold tracking-tight text-ink">Ask AdaptIQ AI</h2><p className="mt-2 text-sm text-slate-500">Your personal AI learning companion.</p></div><div className="grid h-12 w-12 place-items-center rounded-2xl bg-gradient-to-br from-indigo-500 to-violet-600 text-white shadow-glow"><Sparkles size={21} /></div></div><div className="flex flex-1 flex-col justify-end gap-4 py-7" aria-live="polite">{messages.length === 0 ? <div className="m-auto max-w-sm text-center"><div className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-indigo-50 text-primary"><Bot size={28} /></div><h3 className="mt-5 font-display text-xl font-bold text-ink">Where should we start?</h3><p className="mt-2 text-sm leading-6 text-slate-500">Ask a question, share a confusing step, or let your Learning Twin guide the pace.</p></div> : messages.map((message, index) => <div className={`flex items-end gap-3 ${message.role === "user" ? "justify-end" : "justify-start"}`} key={`${message.role}-${index}`}><div className={`grid h-8 w-8 shrink-0 place-items-center rounded-full ${message.role === "assistant" ? "bg-indigo-50 text-primary" : "bg-slate-100 text-slate-600"}`}>{message.role === "assistant" ? <Bot size={15} /> : <UserRound size={15} />}</div><div className={`max-w-[78%] whitespace-pre-wrap rounded-2xl px-4 py-3 text-sm leading-6 ${message.role === "user" ? "rounded-br-md bg-ink text-white" : "rounded-bl-md bg-slate-50 text-slate-700"}`}>{message.content}</div></div>)}{isLoading && <div className="flex items-end gap-3"><div className="grid h-8 w-8 place-items-center rounded-full bg-indigo-50 text-primary"><Bot size={15} /></div><div className="flex items-center gap-2 rounded-2xl rounded-bl-md bg-slate-50 px-4 py-3 text-sm text-slate-400"><LoaderCircle size={15} className="animate-spin" /> Thinking through that...</div></div>}</div><form className="flex items-center gap-2 rounded-2xl border border-slate-200 bg-slate-50 p-2 pl-4 transition focus-within:border-indigo-300 focus-within:bg-white focus-within:ring-4 focus-within:ring-indigo-50" onSubmit={submitQuestion}><input aria-label="Ask AdaptIQ a question" className="min-w-0 flex-1 bg-transparent text-sm text-ink outline-none placeholder:text-slate-400" value={question} onChange={(event) => setQuestion(event.target.value)} placeholder="Ask anything you're learning..." disabled={isLoading} /><button className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-primary text-white shadow-glow transition hover:bg-indigo-700 disabled:cursor-not-allowed disabled:opacity-40" type="submit" aria-label="Send question" disabled={!question.trim() || isLoading}><ArrowUp size={18} /></button></form>{error && <div className="mt-3 rounded-xl border border-rose-100 bg-rose-50 px-3 py-2 text-xs text-rose-700" role="alert">{error}</div>}</section><aside className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><div><div className="text-xs font-bold uppercase tracking-wider text-primary">Get started</div><h2 className="mt-1 font-display text-xl font-bold text-ink">Try asking</h2></div><Sparkles size={18} className="text-violet-500" /></div><div className="mt-6 space-y-2">{starters.map((starter) => <button className="group flex w-full items-center justify-between gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-3.5 text-left text-xs font-semibold leading-5 text-slate-600 transition hover:-translate-y-0.5 hover:border-indigo-200 hover:bg-indigo-50 hover:text-primary" key={starter} onClick={() => setQuestion(starter)} disabled={isLoading}>{starter}<ArrowUp size={15} className="shrink-0 rotate-45 text-slate-300 transition group-hover:text-primary" /></button>)}</div><div className="mt-8 border-t border-slate-100 pt-6"><div className="flex items-start gap-3"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-emerald-50 text-emerald-600"><Bot size={17} /></div><div><strong className="text-sm text-ink">Built around you</strong><p className="mt-1 text-xs leading-5 text-slate-500">Responses adapt to your learning style, confidence, strengths, and recurring mistakes.</p></div></div></div></aside></div>;
}
